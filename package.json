{
  "name": "nuro-receipt-automation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Download NURO 光 invoices, archive them to Google Drive, and record expenses in Google Sheets.",
  "scripts": {
    "start": "node src/index.mjs",
    "oauth": "node scripts/google-oauth.mjs",
    "nuro-login": "node scripts/nuro-login.mjs",
    "check": "node --check src/index.mjs && node --check src/config.mjs && node --check src/google.mjs && node --check src/nuro-v3.mjs && node --check src/pdf.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "googleapis": "^144.0.0",
    "pdf-parse": "^1.1.1",
    "playwright": "^1.54.1"
  }
}
